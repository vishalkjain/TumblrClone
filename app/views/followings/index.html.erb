<h2>Followed Users </h2>

<ul>
  <% @followed_users.each do |user| %>
    <li><%= user.username %> <%= button_to "Unfollow", user_follow_url(current_user.id, user.id), method: :delete %></li>
  <% end %>
</ul>