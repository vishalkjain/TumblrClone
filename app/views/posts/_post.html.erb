<li class="post">
  <%= image_tag post.user.avatar.url(:small), :class => "avatar" %>
  <!-- triangle -->
  <div class="tri"></div>
  <!-- <div id="new-post" class="post-content display-inline-block">
    <h4>Create new post </h4>
  </div> -->

  <ul class="post-content">

    <li><%= link_to post.user.username, user_url(post.user), class: "post-username" %></li>
    <li><%= link_to post.title, post_url(post), class: "post-title" %></li>
    <li><p><%= post.body.html_safe if post.body %></p></li>
  <%#= post.photo_url  if post.photo_url %>
  <%#= post.link_url if post.link_url %>

  <!-- Tags -->
    <li>
      <ul>
        <% post.tags.each do |tag|%>
          <%= link_to "#" + tag.tag_name, tag_url(tag), class: "post-tags" %>
          <% end %>
      </ul>
    </li>

  </ul>
    <!-- Edit/Delete Post -->


  <div class="grey-bar">
    <% if post.user == current_user %>
      <img src="https://s3.amazonaws.com/vishal-tumblr-clone-dev/icons/post_settings_icon.png" class="edit-post-toggle-btn">
    <%end%>
    <div class="edit-options hidden">
      <ul>
        <li class="edit-option">Edit</li>
        <li class="delete-option"><%= button_to("Delete", post_url(post), method: :delete, class: "delete-button")%></li>
      </ul>
    </div>
  </div>

  <div class="edit-post-form-container post-content post-form hidden">
    <form action="<%= post_url(post) %>" method="POST" id="edit-post-form">
      <input type="hidden" name="_method" value="PUT">
      <input type="hidden"
                 name="authenticity_token"
                 value="<%= form_authenticity_token %>">
      <label>
        <input type="text" name="post[title]" value="<%= post.title %>" style="width:475px;">
      </label>
      <br>
      <div class="format-options">
        <img class="format-option" onclick="edit('bold');" src="/assets/editor/iconmonstr-bold-icon-16.png" />
        <img class="format-option" onclick="edit('italic');" src="/assets/editor/iconmonstr-italic-icon-16.png" />
        <img class="format-option" onclick="edit('strikeThrough');" src="/assets/editor/iconmonstr-strikethrough-icon-16.png" />
        <img class="format-option" onclick="edit('justifyLeft');" src="/assets/editor/iconmonstr-align-left-icon-16.png" />
        <img class="format-option" onclick="edit('justifyCenter');" src="/assets/editor/iconmonstr-align-justify-icon-16.png" />
        <img class="format-option" onclick="edit('justifyRight');" src="/assets/editor/iconmonstr-align-right-icon-16.png" />
        <img class="format-option" onclick="edit('insertOrderedList');" src="/assets/editor/iconmonstr-numbered-list-icon-16.png" />
        <img class="format-option" onclick="edit('insertUnorderedList');" src="/assets/editor/iconmonstr-numbered-list-icon-16.png" />
        <img class="format-option" onclick="edit('underline');" src="/assets/editor/iconmonstr-underline-icon-16.png" />

      </div>

      <label>
        <div id="edit-post-area" class="edit-post-area" name="post[body]" contenteditable="true" style="width:475px; height:100px; border: 1px solid black; overflow: hidden" ></div>
        <!-- <textarea name="post[body]"><%= post.body %></textarea> -->
      </label>
        <br>

      <input name="post[body]" class="hidden-post-edit-area" value="<%= post.body %>" style="display:none;">

      <label>Add Tag
        <input type="text" name="tag[tag_name]" style="width: 475px">
      </label>
        <br>

      <ul class="edit-tags">
        <% post.tags.each do |tag|%>

         <li> <%= button_to "#" + tag.tag_name, tagging_url(tag.taggings.find_by_post_id(post.id)), method: :delete, class: "delete-tag" %>
         </li>

        <% end %>
      </ul>
      <br>
      <input type="submit" class="edit-post-buttons" value="Edit Text Post" style="float:left">
      <button type="button" class="edit-post-buttons" style="float:right">Cancel</button>
    </form>
  </div>

</li>