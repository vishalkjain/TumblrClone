<!DOCTYPE html>
<html>
<head>
  <title>Tumblrclone</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <h1 class="logo">tumblr.clone</h1>
    <ul>


    <li><%= link_to "Home", dashboard_url%></li>
    <li><%= link_to "Edit User Info", edit_user_url(current_user.id) %></li>
    <li><%= button_to "Sign out", session_url, :method=>:delete %></li>
  </ul>
  </header>
  <main>
    <%= yield %>
    <div class="sidebar">
      <%= current_user.username %>
      <%= link_to "Create new post", new_post_url %>
      <br>
      <%= link_to "Posts", user_url(current_user.id) %>
      <%= link_to "Followers", user_follows_url(current_user.id) %>

      <%= link_to "Followed Blogs", followings_url %>
      <form action="<%= user_follows_url(current_user) %>" method="POST">
        <input type="hidden"
                 name="authenticity_token"
                 value="<%= form_authenticity_token %>">
        <label>Follow a blog
        <input type="text" name="follow[follow_name]">
        </label>
        <input type="submit" value="Follow">
      </form>
    </div>
  </main>
</body>
</html>
